# SPEC — NN slug

**Size:** XS|S|M|L · **Status:** planned · **Branch:** feat/NN-slug

## Goal

One or two sentences: what this feature delivers and why a user wants it.

## Acceptance criteria

Numbered, each independently verifiable and mappable to a test or a doc.

1. …
2. …
3. `ruff check . && pytest` exits 0.

## Out of scope

What this feature deliberately does **not** do (and where that work lives, if
it is a separate roadmap entry).

## Silent decisions (resolved from SHIP_DECISIONS.md)

Design choices made without a fresh interview, each with its rationale — so a
reviewer can see why, not just what.

- …

## Dev scenarios (tests-first)

The test list. Each scenario names the test and the behaviour it pins down;
these are written red first, then implemented to green.

- `test_…` — …

## Deploy & rollback

Migration / config / data implications, or `n/a — additive, no migration` for a
pure code addition.
